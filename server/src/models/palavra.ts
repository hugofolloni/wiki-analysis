export type Palavra = {
    id: number;
    palavra: string;
    contagem: number;
}

export type Pagina = {
    id: number;
    vetor: string;
    nome: string;
    url: string;
    categoria: string;
}